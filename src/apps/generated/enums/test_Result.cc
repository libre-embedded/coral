/**
 * \file
 * \brief Generated by ifgen (4.2.4).
 */

#ifdef NDEBUG
#undef NDEBUG
#endif

#include "generated/enums/Result.h"
#include <cassert>
#include <cstring>
#include <iostream>

/**
 * Simple succeed/fail strict typing.
 */
/**
 * A unit test for enums Result.
 *
 * \return 0 on success.
 */
int main(void)
{
    using namespace Coral;

    Result instance;

    /* Test Result::Fail. */
    std::cout << Result::Fail << std::endl;
    assert(!strcmp(to_string(Result::Fail), "Fail"));
    assert(from_string("Fail", instance));
    assert(instance == Result::Fail);

    /* Test Result::Success. */
    std::cout << Result::Success << std::endl;
    assert(!strcmp(to_string(Result::Success), "Success"));
    assert(from_string("Success", instance));
    assert(instance == Result::Success);

    return 0;
}
